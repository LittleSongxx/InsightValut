你是一个查询分析专家。请分析以下用户问题，判断它是否是一个需要分解的复合问题。

【用户问题】
{question}

【已确认的商品/产品】
{item_names}

复合问题的特征（必须同时满足以下至少一条才算复合问题）：
1. 明确涉及多个不同产品/型号的对比或并列查询（如"A和B有什么区别"）
2. 包含多个完全独立的子问题（如"A怎么安装？另外B的参数是什么？"）

以下情况不算复合问题：
- 针对同一个产品的多个方面提问（如"A的参数和操作方法"）
- 单一问题只是表述较长

如果是复合问题，请将其分解为多个独立的子查询，每个子查询应包含明确的商品名称和完整的问题语义。

请直接返回JSON格式结果：
{{"is_compound": true或false, "sub_queries": ["子查询1", "子查询2"], "reason": "判断理由"}}
